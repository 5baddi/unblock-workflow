import styled from "styled-components";
import { SIZE } from "../../const";

export const TextMessage = styled.div`
    padding: ${10 / SIZE}em ${16 / SIZE}em ${9 / SIZE}em;
    white-space: pre-line;
`;
